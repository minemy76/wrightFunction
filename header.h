#pragma once

static double p, z, b;

namespace mathFunctions {
	unsigned long long factorial(int n);
	double gammaBef1(double rac);
	double gamma(double a);
	double wrightFunction();
}

namespace userInput {
	double userInput();
}
